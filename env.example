# Configuration Supabase
# URL de votre projet Supabase (obligatoire)
VITE_SUPABASE_URL=https://your-project.supabase.co
# Clé anonyme publique de Supabase (obligatoire)
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Clé Service Role Supabase (serveur uniquement - NE PAS exposer côté client)
# Utilisée par server.mjs pour lire de façon sécurisée les clés BYOK (bypass RLS)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Clé de cryptage pour sécuriser les clés API (obligatoire)
# Doit contenir au moins 32 caractères pour la sécurité AES-256
VITE_ENCRYPTION_KEY=your-secure-encryption-key-32-chars-minimum

# Configuration de l'API Backend (obligatoire)
# URL de votre serveur API Express
VITE_API_URL=http://localhost:3001
# Timeout des requêtes API en millisecondes
VITE_API_TIMEOUT=30000

# Configuration de l'application
VITE_APP_NAME=MediScribe
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=development

# Feature Flags
# Activer Google Analytics ou autre service d'analytics
VITE_ENABLE_ANALYTICS=false
# Activer Sentry pour le monitoring d'erreurs
VITE_ENABLE_SENTRY=false
# Activer les logs de debug dans la console
VITE_ENABLE_DEBUG_LOGS=true

# Limites de fichiers
# Taille maximale des fichiers audio en bytes (25MB par défaut)
VITE_MAX_AUDIO_FILE_SIZE=26214400
# Types MIME autorisés pour les fichiers audio (séparés par des virgules)
VITE_ALLOWED_AUDIO_MIMETYPES=audio/mpeg,audio/wav,audio/webm,audio/m4a

# Mistral AI (optionnel - pour développement uniquement)
# Clé API Mistral pour tests côté frontend (en production, utilisez BYOK stocké dans Supabase)
VITE_MISTRAL_API_KEY=your-mistral-api-key-here
